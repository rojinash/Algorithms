//parameter: multidimentional array of strings
//return an array of all possible combinations of these strings
function helper(arr, currentStr = "", returnArr=[]){
    let len = arr.length;
    for(let i = 0; i < len; i++){

    }
}

function quickBrownFox(arr){
    var retArr = [];
    var row = arr.length;
}